.wrapper_with_padding
  #doc_show
    %h1.title= @doc.title
    %p= simple_format(@doc.content)

    .button
      = link_to "Edit document", edit_doc_path(@doc), class: "button"
      = link_to "Delete document", doc_path(@doc), method: :delete, data: { confirm: "Are you sure?" }, class: "button"